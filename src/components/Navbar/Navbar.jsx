const Navbar = () => {
    return (
        <div>
            <div className="navbar bg-primary text-primary-content mb-5 mt-5">
                <button className="btn btn-ghost text-xl">Todo App</button>
            </div>
        </div>
    );
};

export default Navbar;